# Single setup step: pip install -r requirements.txt
# Installs the course simulator (shipped inside this repo, unmodified) as an
# editable local package, plus everything our learned methods need.

-e ./simulator

torch>=2.1
pyyaml>=6.0
numpy>=1.24
matplotlib>=3.7
pandas>=2.0
tqdm>=4.66
